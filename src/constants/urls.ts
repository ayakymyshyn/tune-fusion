// A bit more complicated file structure might be needed to store more different URLs for different domains
// But currently I don't want to make things too complicated
// It might be easily extended if needed

export const HOME_URL = "/";
export const STATION_DETAILS = "/station/:stationId";
